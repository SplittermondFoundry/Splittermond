import type { Config } from "prettier";

const config: Config = {
    semi: true,
    trailingComma: "es5",
    singleQuote: false,
    printWidth: 120,
    tabWidth: 4,
    useTabs: false,
};

export default config;
