import type { Config } from "prettier";

const config: Config = {
    endOfLine: "auto",
    semi: true,
    trailingComma: "es5",
    singleQuote: false,
    printWidth: 120,
    tabWidth: 4,
    useTabs: false,
    quoteProps: "as-needed",
    bracketSpacing: true,
    bracketSameLine: false,
    arrowParens: "always",
};

export default config;
