const defaultSplinterpointSkillBonus = 3;
const defaultSplinterpointHealthBonus = 5;
const defaultMaxSplinterpoints = 3;

export const splinterpoints = {
    skillBonus: defaultSplinterpointSkillBonus,
    healthBonus: defaultSplinterpointHealthBonus,
    max: defaultMaxSplinterpoints,
} as const;
